# urls.py
from django.urls import path
from .views import ProductListView, ProductDetailView, CategoryListView, DashboardView, register, login_view, about, \
    contact, logout_view, add_to_cart

urlpatterns = (

)
